apiVersion: v1
kind: Service
metadata:
  name: board-backend-service
  namespace: board-app
  labels:
    app: board-backend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 8080
    protocol: TCP
    name: http
  selector:
    app: board-backend 