apiVersion: v1
kind: Service
metadata:
  name: PROJECT_NAME-frontend-service
  namespace: board-app
  labels:
    app: PROJECT_NAME-frontend
spec:
  type: LoadBalancer
  ports:
  - port: 80
    targetPort: 3000
    protocol: TCP
    name: http
  selector:
    app: PROJECT_NAME-frontend 